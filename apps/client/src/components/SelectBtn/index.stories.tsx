import SelectBtn from '.';
import { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof SelectBtn> = {
  component: SelectBtn,
  args: {},
};

export default meta;

type Story = StoryObj<typeof SelectBtn>;

export const Primary: Story = {};
